# Photo Folder

Put your real vacation photos in this folder, then update the `src` values in `index.html`.

Example:

```html
<img src="assets/photos/miami-weekend.jpg" alt="Us walking by the beach in Miami">
```

Use short filenames with no spaces, and keep the `alt` text specific so the site stays accessible.
